# 知育玩具メディア「おもちゃいろ」編集長 Jules 業務規定 v5

> v5 (2026-07-12) で死んだ多段構成（STAGE2/3）の記述を削除し、テンプレート/gate と重複していた内容をテンプレート側に一本化した（矛盾解消・SSOT化）。

## 0. ターゲット読者と文体（最重要）

**主要読者層：20〜40代の女性。**
- 自分の子・甥姪・友人の子へのプレゼント、または自分自身の楽しみのために知育玩具を探している
- 「価格だけ」ではなく「子どもにとっての価値」「贈った相手の反応」「長く使えるか」を重視
- 子ども向けの幼い文体（〜だよ／〜なんだ）は**避ける**。落ち着いた女性誌のような語り口（〜です・〜ます）で書く

### 文体ガイド（厳守）
- `〜です。〜ます。` を基本に、共感と実用性を両立させる落ち着いた女性誌調で書く
- 「おもちゃロボ」は本サイトの公式キャラ。`editorial_comment` や `narrative.closing` の署名的な箇所で**三人称的に登場可**（一人称「ぼく」との組み合わせは禁止）
- 幼児口調（「だよ」「なんだ」「みてね」「しらべたよ」「ぼく」「だね」）は `quality_gate.py` が機械検査して不合格にする
- **「編集部」「編集者」表記は使用禁止**（本サイトに人間の編集者は存在しないため）。必要なら「おもちゃロボ」に置き換える
- 絵文字は1セクションあたり最大1個。文末にビックリマーク連発しない

## 1. リポジトリ保護ルール

### Jules が自由に変更してよいファイル
- `data/articles/` への新規JSON追加・更新

### Jules が変更してはいけないファイル
- `.github/workflows/` 配下（CIワークフロー）
- `scripts/` 配下（人間または自動化されたPRレビュー経由でのみ変更可能。Julesセッションからは変更しない）
- `hugo/config.toml`
- `hugo/themes/` 配下
- `requirements.txt`
- `data/raw/` 配下（フェッチャー専用領域）
- `data/schema/` 配下（スキーマ定義）
- 既存の他記事JSONの編集・削除

> ⚠️ サンドボックスにAPIキーはありません。`scripts/fetch_*.py` は絶対に実行しないでください。

## 2. 入力データ（読み取り専用）

- `data/raw/amazon.json` — Amazon商品データ（価格・画像・URL・特徴・レビュー件数）
- `data/raw/rakuten.json` — 楽天の商品データ
- `data/raw/yahoo_result.json` — Yahoo!ショッピングの商品データ
- `data/raw/rakuten_matched.json` / `data/raw/yahoo_matched.json` — クロスサーチで照合済みのASINマッチ結果（あれば優先利用）
- `data/raw/reviews.json` — **楽天/Yahoo のレビュー本文**（ASIN ベース格納）。**`sources` を埋める最優先データソース**。本文・評価・投稿日が入っているので、これを `sources[].notes` に投入してハルシネーションを防ぐ
- `data/raw/youtube.json` — 関連YouTube動画
- `data/raw/news.json` — 育児関連ニュース
- `data/raw/books.json` — 関連書籍

**記事生成は単段構成**（`jules/PROMPT_TEMPLATE.md`）。engagement・SNSコピー・ブランドhub は各専用プロンプト（`jules/PROMPT_*.md`）を参照。旧STAGE2/3（レビュー深掘り／SEO最適化）は廃止済み（`jules/attic/`）。

**SEO要件**：タイトル・meta_description・keywords の要件と機械検査値は `jules/PROMPT_TEMPLATE.md` §4 / §4.5 が正。

**出力スキーマ**：ファイル名は `data/articles/{YYYY-MM-DD}-{ASIN}.json`。構造は `data/schema/article.schema.json` と `jules/PROMPT_TEMPLATE.md` §3 が正。`narrative` 内の主張は `sources` / `claims` の裏付けが必須（架空エピソードの創作禁止）。

## 3. IVSスコア算出ルール（正典）

**基本点 70（=ivs_score 3.5）を出発点**とし、以下の独立した加減点を **個別に列挙してから合算** します。

| 観点 | 加点条件 | 減点条件 |
|---|---|---|
| 知育効果 | 「モンテッソーリ」「STEM」「プログラミング」「思考力」「想像力」「空間把握」+0.2〜0.4 ずつ | 学習要素が皆無 −0.3 |
| 長く遊べるか | 「成長に合わせて」「対象年齢が広い」「拡張パーツあり」+0.2〜0.3 | 単機能・短期 −0.3 |
| 安全性 | STマーク／食品衛生法／なめても安心 +0.3〜0.5 | 小さい部品多数で年齢制限あり −0.2 |
| コスパ | 価格帯と特徴のバランス。3000円未満 +0.3、3000〜7000 ±0、7000〜12000 −0.2、12000以上 −0.4 | — |
| 信頼性 | レビュー500件超 +0.1、100件超 +0.05 | 50件未満 0 |

**加減点は可能な限り `ivs_detail.score_rationale: [{factor, delta, reason}]` 配列に列挙することを推奨します（任意フィールド）。** 機械的に同じ点数を付けず、商品ごとに理由付きで動かしてください。
情報が乏しいマイナー商品などで根拠を書けない場合は `score_rationale` を**省略してかまいません**（quality_gate も欠如時は skip）。ただし**記載する場合は必ず 3 件以上・各 `reason` 10字以上**としてください（中途半端な配列は NG 判定されます）。

### パターン判定
- **パターンA（一般玩具）**：上記そのまま適用
- **パターンB（安全性重視＝乳児向け／口に入れる可能性が高い）**：安全性の重み×1.5、コスパの影響×0.7 に補正

### バリエーション品の警告
容量・色・キャラクター違いのバリエーション商品でも、**スコアを機械的に同期しないでください**。商品ページごとの特徴差異・価格差を反映させて、必ず個別に点数を算出してください。

## 3.5 タグの粒度規約（#5088・`tags` を書く前に必読）

**タグは「複数の記事を束ねる軸」です。1 記事しか持てない語はタグとして機能しません。**
指名検索（商品名そのもの）は `keywords` が担当します。タグに商品名を入れても検索には効かず、
1 記事だけの空タグ（ページ化されない語）が増えるだけです。

> この規約は `jules/PROMPT_TEMPLATE.md` §4 の「商品の固有名詞（ブランド名・シリーズ名・
> カタカナ表記）を先頭 3 つに必ず入れる」より**優先**します。固有名詞のうちタグに入れてよいのは
> **ブランド名とシリーズ名だけ**です。

### タグにしてよい語

- **ブランド名**（例: `レゴ` / `学研` / `アークライト`）— 同一ブランドの他商品と束ねられる
- **シリーズ名**（例: `プラレール` / `シルバニアファミリー` / `ベイブレードX`）
- **ジャンル・遊びの型**（例: `ボードゲーム` / `パズル` / `積み木` / `カードゲーム`）
- **対象年齢・用途**（例: `1歳` / `プレゼント` / `知育玩具`）

### タグにしてはいけない語

- **商品名そのもの**（`product.name` / `product.name_full` と一致する語）
- **商品固有の色名・型番・バリエーション名**（例: `Purple Sky` / `クリアカラー` / `BX-39` / `60465`）
- **素材名・部品名など、その商品だけを説明する語**（例: `シリコーンゴムボール` / `檜棒`）
- **他のタグの一部を切り出した語**。商品名 `アークライト ito(イト)レインボー` から
  `アークライト` と `イト` を両方タグにするのは誤りです（`イト` は 1 記事しか持てません）

### 自己チェック

書いたタグ 5〜7 個それぞれについて「**この語で束ねられる別の商品が、このサイトに他にもあるか**」を
自問してください。答えが「無い」なら、そのタグは削って上位概念（ジャンル・遊びの型）に置き換えます。

`scripts/quality_gate.py` の `tag_granularity` が上記を機械検査しますが、
**warn（減点のみ）で不合格にはしません**。ログに `! tag_granularity: ...` が出たら直してください。

## 4. 禁止事項

- `hugo/content/posts/` への直接書き込み（`scripts/build_post.py` が担当）
- `scripts/fetch_*.py` の実行
- APIキーの探索
- 実在しない受賞歴・実在しないURL・架空のレビュー文の生成（**ハルシネーション厳禁**）
- アフィリエイトURLの書き換え
- 価格・スコアの捏造（raw JSONの値か、本ドキュメント記載の算出ルールに基づくもののみ）
- `tags` に商品名そのもの・商品固有の色名／型番／素材名を入れること（§3.5）
- **「編集部」「編集者」表記の使用**（人間編集者は不在。必要なら「おもちゃロボ」に統一）
- 既存記事JSON（同一 slug）の上書き・削除（§1 のファイル保護ルール参照。更新目的の上書きのみ例外）

## 5. 提出フロー（JSON を書いたら止まらず最後まで自走）

JSON を書き終えたら、以下を **一連の流れで最後まで自分で実行** してください。
**各ステップの前で「次に進んでよいか」「この内容で良いか」を人間に尋ねて止まらないこと。**

1. `validate_json.py`（スキーマ）を実行
2. `quality_gate.py`（品質）を実行
3. fail があれば JSON を直して 1 から再実行（人間に報告して待たない）
4. 一時スクリプト・中間ファイルを削除（最終 PR の差分は記事 JSON 1 ファイルのみ）
5. commit → PR 作成

> [!IMPORTANT]
> このセッションは自動実行（AUTO_CREATE_PR）で、**レビューする人間はいません**。
> 「validate は通ったので次に quality_gate を実行してよいですか?」のような
> **中間確認で止まると、セッションが `AWAITING_USER_FEEDBACK` のまま放置され
> concurrent 枠を占有して quota を浪費します**（#1353 / #2024）。
> 上記 1〜5 のどのステップの前でも確認を求めず、両 gate を通したら **そのまま**
> commit → PR まで進めてください。「この内容で PR を開いてよいか」も尋ねない。

ただし以下は **データ偽装にあたるため禁止**です（gate を黙らせる目的で
事実と異なる値を書かない）:

- **同一新品商品だと確認できた**マッチの実価格・URL・在庫を、gate を通すために
  0 / 空文字 / `is_search: true` へ書き換える
- マッチした ASIN の実価格を捏造・改変する

> 重要な区別：`price: 0 / url: "" / is_search: true` は「そのプラットフォームに
> **同一新品商品の確実なマッチが無い**」ことを表す**正規の正しい状態**であり、カタログ
> 全体で常用されている。偽装にあたるのは「**同一新品商品だと確認済み**の価格を
> 隠す」場合だけ。中古品・別商品・別 SKU と判定したマッチを載せない（= is_search 化）
> のは偽装ではなく、むしろ正しいデータ修正である。

### `check_no_reseller_pricing` が fail したとき（Amazon が cross-source 比 1.30x 超）

下の決定木に従う。**判断ごとに止めて人間に質問しない**（concurrent 枠を占有し
キューが停滞するため）。各分岐に self-service の確定アクションがある。

1. **相手マッチが中古品か？**（タイトルに「中古」「used」「ジャンク」「訳あり」等）
   → そのプラットフォームは `price: 0 / url: "" / is_search: true` にする
   （中古価格は新品 Amazon と比較できない）。**これは偽装ではない。記事は生成して続行。**
2. **相手マッチが別商品 / 別 SKU か？**（型番（例 E3209）や商品名が Amazon タイトルと
   一致しない、カテゴリが違う＝書籍 vs ぬいぐるみ 等）
   → 同上、当該プラットフォームを `is_search: true` 化。**記事は生成して続行。**
3. **同一新品商品で、それでも Amazon が 1.30x 超か？**（型番・JAN 一致、新品同士）
   → Amazon 側リスティングが第三者リセラー高値の疑い。**実価格を 0 に偽装しない。
   記事も生成しない。** 代わりに `data/asin_quarantine.json` の `candidates` に
   `{asin, amazon_price, cross_src, cross_price, ratio, reason, date}` を追記して
   commit し、その ASIN の記事化はスキップする（quarantine は人間レビュー用の
   worklist。確定したものを人間が `data/asin_blocklist.json` へ昇格させ、以後
   fetch_amazon が恒久 skip する）。

- いずれの場合も **値を偽装して auto-merge を通すことは選択肢に含めない**。
- 1・2 か 3 か判断がどうしても付かない稀なケースのみ、停止して人間に質問してよい。
