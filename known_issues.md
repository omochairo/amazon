# 既知の課題と副作用ケース (Known Issues)

## 1. 検索キーワード抽出におけるひらがな固有名詞の助詞誤判定 (V12アプローチ)

### 背景と課題
PA-API向けの検索キーワードを生成する `build_search_keyword` において、`口コミ` や `最安値` などの不要な filler 語を削除しつつ、`積み木` や `あそび方` などのひらがなを含む固有名詞を正しく抽出するため、V12アプローチでは「ひらがなが2文字以上続いた直後にある助詞を切り離す」ロジックを採用しています。

しかし、このルールを単純に適用すると、語尾に助詞と同等の文字（の、に、と、を、は、が、や、も、で、へ）を持つひらがな固有名詞が誤って分割され、語尾が削られてしまう副作用が発生します。

### 副作用が発生した代表的な事例（計7件、5単語）
252記事を対象にした検証にて、以下のひらがな固有名詞が誤って分割される副作用が確認されました。

1. **のりもの** → `のりも` + `の` に分割され、`のりも` が抽出される (2件)
2. **おままごと** → `おままご` + `と` に分割され、`おままご` が抽出される (2件)
3. **いつまで** → `いつま` + `で` に分割され、`いつま` が抽出される (1件)
4. **おかいもの** → `おかいも` + `の` に分割され、`おかいも` が抽出される (1件)
5. **なに** → `な` + `に` に分割され、単語長が2文字未満のためスキップされる (1件)

### 対策と設計
現在、これらの副作用を防ぐために、`scripts/competitor_ranking.py` に例外辞書 `_EXCEPTION_WORDS` を定義し、分割処理の対象外としています。

```python
# 助詞切り離し処理で誤判定（副作用）を起こすひらがな固有名詞等の例外辞書
_EXCEPTION_WORDS = {
    "のりもの", "おままごと", "いつまで", "おかいもの", "なに"
}
```

## 2. 将来の拡張・改善設計 (V13以降)

将来的に10,000記事規模に拡大した際、上記以外のひらがな固有名詞（例: 「おすし」「おままごと」「ぬりえ」等のひらがな固有名詞で語尾が助詞に該当するもの）が新たに追加され、副作用件数が増加する可能性があります。

そのため、V13以降で容易に改善できるよう以下の拡張設計を採用しています。

1. **例外辞書 `_EXCEPTION_WORDS` による一元管理**
   ひらがな固有名詞のリストを集合（`set`）形式で定義しており、追加・削除が $O(1)$ の時間計算量で行えます。
2. **将来的な外部設定ファイル化への対応**
   必要に応じて `_EXCEPTION_WORDS` を `config/exceptions.json` などの外部ファイルから読み込むように容易にリファクタリング可能です。
