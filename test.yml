name: Test
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: stefanzweifel/git-auto-commit-action@v5
        if: always()
        with:
          commit_message: "data: 商品データの自動更新"
